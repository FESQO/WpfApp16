<Page x:Class="WpfApp15.Pages.EditPrepodPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditPrepodPage">
    <StackPanel>
        <StackPanel DataContext="{Binding EditPrepod}">
            <Label Content="Имя"/>
            <TextBox Text="{Binding firstNameP}"/>
            <Label Content="Фамилия"/>
            <TextBox Text="{Binding lastNameP}"/>

        </StackPanel>
        <Button Content="Сохранить и закрыть" Command="{Binding SavePrepod}" />
    </StackPanel>
</Page>
